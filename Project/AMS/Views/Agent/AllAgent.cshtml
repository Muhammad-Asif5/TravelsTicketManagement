@model IEnumerable<AMS.Models.Agent>

@{
    Layout = null;// "~/Views/Shared/_Layout.cshtml";
}

<table id="myTable" class="table table-bordered table-hover">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Agent_ID)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Agent_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agent_Status)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @{
            int counter = 0;// Index start value
            foreach (var item in Model)
            {
                <tr id="@item.Agent_ID">
                    @*<td>
                        @Html.DisplayFor(modelItem => item.Agent_ID)
                    </td>*@
                    <td>
                        @Html.DisplayFor(modelItem => item.Agent_Name)
                    </td>
                    <td>
                        @{
                            if (item.Agent_Status == "1")
                            {
                                <label class="badge bg-success">Active</label>
                            }
                            else
                            {
                                <label class="badge bg-danger">Closed</label>
                            }
                        }
                    </td>
                    <td>
                        <button onclick="getById(this.id)" id="@item.Agent_ID" class="btn btn-primary btn-sm">Edit</button>
                        <button onclick="deleteById(this.id)" id="@item.Agent_ID" class="btn btn-danger btn-sm">Delete</button>
                    </td>
                </tr>
                counter++;
            }
        }


    </tbody>
</table>

